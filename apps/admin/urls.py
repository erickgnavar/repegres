from django.conf.urls import patterns, url

urlpatterns = patterns('',
    # url(
    #     regex=r'^product/(?P<pk>\d+)/edit/$',
    #     view=p.ProductUpdateView.as_view(),
    #     name='super_admin_product_edit'
    # ),
    # url(
    #     regex=r'^product/(?P<pk>\d+)/delete/$',
    #     view=p.ProductDeleteView.as_view(),
    #     name='super_admin_product_delete'
    # ),
)
